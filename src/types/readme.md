# Types Directory

This directory contains global TypeScript type definitions and ambient module augmentations.

## Structure
- `next-auth.d.ts`: Extended NextAuth types for JWT tokens, user sessions, and custom authorization fields.
- Additional `.d.ts` and shared domain types can be added here.
