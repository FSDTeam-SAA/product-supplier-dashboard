# Hooks Directory

This directory contains reusable, global React hooks used across multiple features.

## Available Hooks
- `use-debounce.ts`: Debounce dynamic input values (e.g. search filters, rapid form edits).
- `use-media-query.ts`: Detect and react to CSS media queries (e.g. responsive mobile/desktop breakpoints).
