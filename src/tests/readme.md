# Tests Directory

This directory contains unit and integration test files (e.g. Jest / React Testing Library).

## Guidelines
- Place unit tests for utility functions, custom hooks, and isolated UI components here or co-located next to feature files with `.test.ts` / `.test.tsx` extensions.
- Run tests via `pnpm test` or `npm test`.
